[project]
name = "django-nplus1-example"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = ["Django>=6", "django-nplus1"]

[dependency-groups]
dev = ["pytest", "pytest-django"]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "settings"
pythonpath = ["."]
